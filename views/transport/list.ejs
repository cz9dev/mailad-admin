<!-- views/transport/list.ejs -->
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <a href="/transport/new" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Nueva Regla
                </a>
            </div>

            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">Reglas de Transporte/Reenvío</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Patrón</th>
                                    <th>Destino</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (rules.length> 0) { %>
                                    <% rules.forEach(function(rule) { %>
                                        <tr>
                                            <td>
                                                <code><%= rule.pattern %></code>
                                            </td>
                                            <td>
                                                <code><%= rule.destination %></code>
                                            </td>
                                            <td>
                                                <div class="btn-group btn-group-sm">
                                                    <a href="/transport/<%= encodeURIComponent(rule.pattern) %>/edit"
                                                        class="btn btn-outline-primary" title="Editar regla">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                    <form
                                                        action="/transport/<%= encodeURIComponent(rule.pattern) %>/delete"
                                                        method="POST" class="d-inline">
                                                        <button type="submit" class="btn btn-outline-danger"
                                                            onclick="return confirm('¿Estás seguro de eliminar la regla para <%= rule.pattern %>?')"
                                                            title="Eliminar regla">
                                                            <i class="bi bi-trash"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                        <% }); %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="3" class="text-center py-4">
                                                        <div class="text-muted">
                                                            <i class="bi bi-inbox display-4 d-block mb-2"></i>
                                                            No hay reglas de transporte configuradas.
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="card-title mb-0">Información</h6>
                </div>
                <div class="card-body">
                    <p>Las reglas de transporte definen cómo Postfix maneja el correo para diferentes destinos:</p>
                    <ul>
                        <li><strong>Patrón</strong>: Dominio, subdominio o patrón específico</li>
                        <li><strong>Destino</strong>: Servidor de destino, transporte local, etc.</li>
                    </ul>
                    <p class="mb-0"><strong>Ejemplos:</strong></p>
                    <ul class="mb-0">
                        <li><code>example.com</code> → <code>smtp:[mail.example.com]</code> - Reenvía todo el dominio
                        </li>
                        <li><code>.example.com</code> → <code>smtp:[mx.example.com]</code> - Reenvía dominio y
                            subdominios</li>
                        <li><code>user@example.com</code> → <code>local:user2</code> - Reenvía a usuario local</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>